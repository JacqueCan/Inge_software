@model IEnumerable<APIWEB_UMG.Models.tbl_paquete>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tipo_Paquete)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_Paquete)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Paquete }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Paquete }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Paquete })
        </td>
    </tr>
}

</table>
