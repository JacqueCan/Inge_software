@model IEnumerable<APIWEB_UMG.Models.tbl_tipoPersona>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tipo_Persona)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_Persona)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Tpersona }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Tpersona }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Tpersona })
        </td>
    </tr>
}

</table>
